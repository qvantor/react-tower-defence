import * as constants from './weapons.constants'
import Model from './weapons.model'

export default function weapons (state = Model, { type, payload }) {
  switch (type) {
    default:
      return state
  }
}
