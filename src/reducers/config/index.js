import * as constants from './config.constants'
import Model from './config.model'

export default function config (state = Model, action) {
  switch (action.type) {
    default:
      return state
  }
}
