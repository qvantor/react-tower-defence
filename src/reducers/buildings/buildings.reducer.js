import * as constants from './buildings.constants'
import Model from './buildings.model'

export default function buildings (state = Model, { type, payload }) {
  switch (type) {
    default:
      return state
  }
}
