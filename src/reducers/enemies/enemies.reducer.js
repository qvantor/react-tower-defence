import * as constants from './enemies.constants'
import Model from './enemies.model'

export default function enemies (state = Model, { type, payload }) {
  switch (type) {
    default:
      return state
  }
}
