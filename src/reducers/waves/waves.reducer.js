import * as constants from './waves.constants'
import Model from './waves.model'

export default function waves (state = Model, { type, payload }) {
  switch (type) {
    default:
      return state
  }
}
